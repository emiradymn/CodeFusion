@model List<Post>

    <div class="card mt-3">
        <div class="card-header">
            <h5 class="card-title">Yeni gönderi</h5>
        </div>
        <div class="card-body">
            @foreach(var post in Model)
            {
            <div class="mb-2">
                <h6><a href="/posts/details/@post.Url">@post.Title</a></h6>
            </div>
            }
        </div>
    </div>